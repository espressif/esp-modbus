version: "0.0.2"
description: ESP-MODBUS is the official Modbus library for Espressif SoCs.
dependencies:
  idf: ">=4.1"
files:
  exclude:
    - "docs/_build/**/*"
    - "docs/_build"
    - "test/**/build/**/*"
    - "test/**/build"
